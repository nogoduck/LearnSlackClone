import React from "react";
import Workspace from "../../layouts/Workspace";

const Channel = () => {
  return (
    <Workspace>
      <div>로그인을 성공했습니다.</div>
    </Workspace>
  );
};

export default Channel;
